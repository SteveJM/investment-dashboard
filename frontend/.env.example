# Local `npm run dev` only. Leave unset for the production/Docker build -
# there the app calls the API as same-origin relative paths (/api/...) and
# nginx injects the Authorization header, so the API key never ships in the
# browser bundle. See frontend/README.md.

VITE_API_BASE_URL=http://localhost:4000
VITE_API_KEY=devkey
